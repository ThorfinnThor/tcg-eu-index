name: archive-future

on:
  workflow_dispatch: {}

jobs:
  archive_future:
    runs-on: ubuntu-latest
    steps:
      - name: MVP note
        run: |
          echo "Archive workflow is intentionally parked for the MVP."
          echo "Enable this after Cardmarket download URLs and object storage are finalized."
