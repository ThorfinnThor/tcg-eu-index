name: audit-future

on:
  workflow_dispatch: {}

jobs:
  audit_future:
    runs-on: ubuntu-latest
    steps:
      - name: MVP note
        run: |
          echo "Archive audit is intentionally parked for the MVP."
          echo "Enable this after the raw daily archive exists."
