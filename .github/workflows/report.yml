name: report-future

on:
  workflow_dispatch:
    inputs:
      week:
        description: ISO week, for example 2026-W35
        required: false

jobs:
  report_future:
    runs-on: ubuntu-latest
    steps:
      - name: MVP note
        run: |
          echo "Weekly report automation is intentionally parked for the MVP."
          echo "Enable this after real daily data is flowing."
