[project]
name = "tcg-eu-index-engine"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["click>=8.1", "polars>=1.9", "pyyaml>=6.0", "requests>=2.32"]

[project.scripts]
indexengine-images = "indexengine.card_images.cli:main"

[tool.setuptools.packages.find]
where = ["."]
