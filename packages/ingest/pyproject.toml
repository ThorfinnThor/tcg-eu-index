[project]
name = "tcg-eu-index-ingest"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["tcg-eu-index-core", "click>=8.1", "polars>=1.9", "pyyaml>=6.0", "requests>=2.32"]

[tool.setuptools.packages.find]
where = ["."]
