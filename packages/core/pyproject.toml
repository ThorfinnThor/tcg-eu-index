[project]
name = "tcg-eu-index-core"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["boto3>=1.34", "psycopg[binary]>=3.2", "pydantic>=2.8", "requests>=2.32"]

[tool.setuptools.packages.find]
where = ["."]
