{
  "name": "tcg-eu-index-monorepo",
  "private": true,
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "lint": "npm --prefix apps/web run lint",
    "test:web": "npm --prefix apps/web run test",
    "test": "uv run pytest",
    "check": "uv run ruff check . && uv run mypy && npm --prefix apps/web run lint && npm --prefix apps/web run test"
  },
  "engines": {
    "node": ">=20"
  }
}
